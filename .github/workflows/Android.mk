# === Added for loc_log.h include path ===
LOCAL_C_INCLUDES += \
    $(LOCAL_PATH)/../utils \
    $(LOCAL_PATH)/utils
# Added for auth fixbeat.cpp include usr ===
STRING_PY_BEFORE +x /
    @vulkan{
step~ssh & :0 i3
   }