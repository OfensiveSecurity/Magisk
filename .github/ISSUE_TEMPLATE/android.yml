- name: Install Dependencies
  run: |
    sudo apt-get update
    # Forzamos la resolución de conflictos de base-files si aparecen
    sudo apt-get install -y --fix-broken
    sudo apt-get install -y build-essential libstdc++6
